ff14huntlist.online {
    root * /usr/share/caddy
    file_server
    encode gzip

    # SPA fallback — serve index.html for all non-file routes
    @notFile {
        not file
    }
    rewrite @notFile /index.html
}
